import React from 'react';
import ReactDOM from 'react-dom/client';
import ReferralWebApp from './ReferralWebApp';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
      <ReferralWebApp />
    
  </React.StrictMode>
);

